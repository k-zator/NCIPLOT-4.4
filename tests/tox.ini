[tox]
envlist = conda
isolated_build = false
skip_missing_interpreters = true

[testenv]
description = Run tests using the environment.yml-derived conda env only
skip_install = true
allowlist_externals = conda
commands =
    conda run -n nciplot-4.4 python -m pytest -q
